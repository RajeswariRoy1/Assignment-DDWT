@using WebMatrix.Data
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
<script src="../Scripts/jquery-3.5.1.js"></script>
<script src="../Scripts/bootstrap.js"></script>
<script src="../Scripts/popper.js"></script>
@{
    var db = Database.Open("FoodDB");
    var sql = "select * from orders as o left join stores as s on o.store_id=s.store_id where o.order_id LIKE CONCAT(@0, '%') or o.order_status LIKE CONCAT(@0,'%') or o.order_amount LIKE CONCAT(@0,'%')";
    var output = db.Query(sql, Request.Form["SearchOrder"]);
}
<main class="Container">
    <div class="jumbotron pt-1 pb-1 mt-2 ">
        <h2 class="text-center">All Orders</h2>
    </div>
    <div class="row">
        <form method="post" action="/Home/Index">
                <div class="col-6 mb-1">
                    <input type="text" name="SearchOrder" class="form-control" placeholder="SearchOrder" />
                </div>
                <div class="input-group-append">
                    <button class="btn btn-primary" type="submit">Search By Order Status</button>
                </div>
            </div>
        </form>
    </div>

    @if (output.Count() > 0)
    {
        <div class="row">
            @foreach (var i in output)
            {
                <div class="card" style="width: 18rem; margin:12px; padding:0 0">
                    <h4 class="card-header">Order_ID@i.order_id</h4>
                    <div class="card-body">
                        <img class="card-img-top" src="https://storage.googleapis.com/proudcity/mebanenc/uploads/2021/03/placeholder-image.png" alt="Card image cap">
                        <p class="card-title">Order_Status:@i.order_status</p>
                        <p class="card-text">Order_Amount:@i.order_amount.ToString("C")</p>
                        <p class="card-text">Channel_ID:@i.channel_id</p>
                        <h6>Store Details</h6>
                        <p class="card-text">Store_Name:@i.store_name</p>
                        <p class="card-text">Store_Segment:@i.store_segment</p>
                        <p class="card-text">Store_Plan_Price:@i.store_plan_price</p>
                        <a href="/Home/Details?id=@i.order_id" class="btn btn-primary">See details</a>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <h2 class="text-danger text-center">Oops! No related orders found...</h2>
    }
</main>
